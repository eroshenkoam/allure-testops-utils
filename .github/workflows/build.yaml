name: Build

on:
  pull_request:
    branches:
      - '*'
  push:
    branches:
      - 'master'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'gradle'
      - run: ./gradlew build
      - run: ./gradlew prepareDockerfile
      - uses: docker/build-push-action@v5
        with:
          push: true
          context: build/docker
          tags: ghcr.io/eroshenkoam/allure-testops-utils-cstm:${{ github.run_id }}
